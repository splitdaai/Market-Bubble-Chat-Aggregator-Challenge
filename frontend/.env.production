# Baked into production builds (npm run build) so the hosted demo's OAuth
# "Connect" flow + /auth/config talk to the live backend on AWS.
# Local dev (npm run dev) ignores this and uses .env.local (localhost) instead.
VITE_BACKEND_URL=https://3-213-104-77.nip.io
