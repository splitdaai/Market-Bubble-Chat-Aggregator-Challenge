node_modules/
dist/
.env
.env.local
*.local
.DS_Store
*.log
.vite/

# backend runtime
backend/data/
auth-store.json
*.env.local

# Visitor tracking lives entirely on the AWS edge (Caddy access logs +
# an EC2-only parser → /visits dashboard). No tracking code or data in git.
visits.log
**/visits.log

# TS incremental build cache
*.tsbuildinfo
